using UnityEngine;
using System.Collections;

public class SpeedShow : MonoBehaviour {

	public float yoffset;
	public float xoffset;
	//public Texture2D texture;
	//public Texture2D gauge;

	void Start()
	{
	//	texture = (Texture2D)Resources.Load ("speed");
	//	gauge = (Texture2D)Resources.Load("speedgöster");

	//	guiText.pixelOffset = new Vector2 (-(Screen.width / 2) + xoffset, yoffset);
	
		}
	void Update()
	{
	//	this.guiText.pixelOffset. (this.guiText.pixelOffset.x - (Screen.width / 2) + 10, Screen.height / 100);
	}
	void OnGUI()
	{

//		GUI.DrawTexture (new Rect (100, 350, 100, 100), texture);
	//	GUI.DrawTexture (new Rect (115, 394, 50, 12), gauge);
		}
}
